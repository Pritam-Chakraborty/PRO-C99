from importlib.resources import path
import os
import shutil
import time
def converttime():
    ctime = os.stat(path).st_ctime
    return ctime
converttime()
os.path.exists(path)
os.walk(path)
os.path.join()
os.stat(time.ctime)
